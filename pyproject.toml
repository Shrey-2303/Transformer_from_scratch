[project]
name = "transdformers"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "numpy",
    "einops",
    "tqdm",
    "datasets",
    "tokenizers",
]

[tool.uv]
package = false